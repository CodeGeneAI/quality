{
  "name": "quality-fixture-jsonc",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist build lib out coverage .nyc_output .turbo .vite .cache node_modules tsconfig.tsbuildinfo tsconfig.build.tsbuildinfo tmp temp"
  }
}
